{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "lerna run build",
    "deploy": "lerna run deploy --parallel",
    "force-update-cluster": "cd kubernetes && kubectl rollout restart -f .",
    "dev": "lerna run dev --parallel"
  },
  "license": "MIT",
  "devDependencies": {
    "lerna": "^4.0.0",
    "prettier": "^2.3.2",
    "typescript": "^4.4.2"
  }
}
